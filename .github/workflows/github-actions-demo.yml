name: GitHub Actions Demo
env:
  test_version: "0.0.0"
  in_version: "1.1.1.1.1.1"
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  version-check:
    runs-on: ubuntu-latest
    steps:
      - name: Version Check
        id: version_check
        run: |
          TEST="${{ env.in_version }}"
          TEST2="(${{ env.in_version }} =~ ^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-((0|[1-9][0-9]*|[0-9]*[a-zA-Z-][0-9a-zA-Z-]*)(\.(0|[1-9][0-9]*|[0-9]*[a-zA-Z-][0-9a-zA-Z-]*))*))?(\+([0-9a-zA-Z-]+(\.[0-9a-zA-Z-]+)*))?$)"
          if [[ TEST1 =~ TEST2 ]]; then
              echo "Valid Version String"
          else
              echo "Not valid version string: " ${{ TEST2 }}
              exit 1
          fi
          
      - name: Next Item
        id: next_item        
        run: echo "Hello, World!"
